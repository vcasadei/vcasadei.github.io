## 1. Switch the build step off actions/jekyll-build-pages

- [x] 1.1 Replace `.github/workflows/deploy.yml`'s `build` job step with `ruby/setup-ruby` (`bundler-cache: true`) followed by `bundle exec jekyll build --destination ./_site`; verify the workflow YAML is valid and the job still uploads the artifact from the same destination the `deploy` job expects — done; also pinned `ruby-version: '3.3'` explicitly (my local lockfile was generated under a very new local Ruby 4.0, so pinning a broadly-available CI version is safer than relying on the runner's default) and set `JEKYLL_ENV: production` on the build step. **Finding**: the theme's `comments.html` gates the whole giscus embed on `jekyll.environment != 'development'` — the old wrapped action set `JEKYLL_ENV=production` internally, so this had to be replicated explicitly or comments would silently stop rendering
- [x] 1.2 Update the `Gemfile`: replace `gem "github-pages"` with a pinned `gem "jekyll"` version, add `gem "jekyll-paginate-v2"`, and add explicit `gem` lines for any other plugin the site currently relies on that was previously pulled in transitively by the `github-pages` metagem (check the current `Gemfile.lock` for what's actually in use: `jekyll-remote-theme`, `jekyll-feed`, `jekyll-seo-tag`, `jekyll-sitemap`, etc.) — done: `jekyll ~> 4.3`, `jekyll-feed`, `jekyll-paginate-v2`, `jekyll-remote-theme`, `jekyll-sitemap`, `jemoji`, `webrick` (checked `_config.yml`'s actual `plugins:` list, not the full github-pages metagem — only 5 plugins were ever actually active: feed/paginate/remote-theme/sitemap + jemoji; `jekyll-seo-tag` was never used). **Finding**: `jekyll-TeXt-theme`'s own gemspec has a hard runtime dependency on classic `jekyll-paginate` (`Jekyll::PluginManager#require_theme_deps` fails a `LoadError` without it) even though the site no longer activates it as a plugin — kept `gem "jekyll-paginate"` installed (with a comment explaining why) purely so that `require` succeeds; its own generator stays inert since `_config.yml` no longer sets the legacy `paginate:` count key
- [x] 1.3 Regenerate `Gemfile.lock` against the new `Gemfile`; verify `bundle install` succeeds locally or in CI — done locally (`bundle install` succeeded, resolving a broad `PLATFORMS` list that already includes `x86_64-linux-gnu`, the typical GitHub-hosted runner arch). **Major finding**: this also restored the ability to run real local Jekyll builds for the first time all session — the old `github-pages` gem pinned `liquid` to a version incompatible with modern Ruby (`undefined method 'tainted?'`); the new `jekyll ~> 4.3` resolves `liquid 4.0.4`, which works fine. Used this to locally verify every remaining task below before ever pushing to CI

## 2. Add locale/pt-listing fields to posts and defaults

- [x] 2.1 Add `locale: en` to `_config.yml`'s existing `_posts` (root, English) `defaults:` scope, and `locale: pt-BR` to the existing `_posts/pt` `defaults:` scope — same mechanism already used for `lang`, no per-post front-matter changes needed — done
- [x] 2.2 Write `_plugins/pt_listing_tag.rb`: a Jekyll generator (registered to run before `jekyll-paginate-v2`'s own generator) that tags every post with a synthetic `pt-listing` category when it belongs in the Portuguese listing — a Portuguese post always qualifies; an English post qualifies only when no Portuguese post shares its `translation_key` — mirroring `_includes/article-list.html`'s existing Liquid dedup rule exactly; verify with a unit-level check (e.g. a small script or `jekyll build` + inspecting generated category data) that the 5 translated English posts do NOT get tagged and any hypothetically untranslated English post would — done, `priority :high` (runs before v2's own `priority :lowest` generator; confirmed `Jekyll::Site#categories` is computed lazily and memoized on first access, so as long as my generator runs first the category hash reflects my tagging). Verified locally: built with a temporary untranslated test post (no `translation_key`) — it correctly appeared on both the English listing (native) and the Portuguese listing (fallback), while all 5 real translated English posts correctly did NOT appear on the Portuguese listing (only their Portuguese siblings did). Test post removed after verification, not committed

## 3. Configure jekyll-paginate-v2

- [x] 3.1 Add the site-wide `pagination:` block to `_config.yml` (`enabled: true`, `per_page: 8`, `permalink` matching today's `/page:num`, `sort_field: date`, `sort_reverse: true`) — done. **Finding**: jekyll-paginate-v2 refuses to run at all ("Legacy jekyll-paginate configuration compatibility mode has expired") if the legacy `paginate:` (per-page count) key is still set in `_config.yml` — removed it. Kept `paginate_path: /page:num`, since that specific key is only read by the theme's own `paginator.html` for a couple of jump-to-page links and isn't part of jekyll-paginate-v2's legacy-detection check (verified by reading the plugin's source)
- [x] 3.2 Add `pagination: { locale: en }` to `index.html`'s front matter and remove its `articles.data_source: site.posts` override — done
- [x] 3.3 Add `pagination: { category: 'pt-listing' }` to `pt/index.html`'s front matter and remove its `articles.data_source: site.posts` override — done
- [x] 3.4 Update `_includes/paginator.html`: remove the `_page_articles_source != 'site.posts'` stopgap guard; simplify the custom `_post_count` loop now that `paginator.posts`/`paginator.total_pages` are already correctly scoped per page (verify `article-list.html`'s existing per-item title/link fallback logic - e.g. `title_pt`, English-URL fallback for an untranslated post appearing on the Portuguese listing via `pt-listing` - still applies correctly to whatever `paginator.posts` now contains) — done; replaced the manual dedup-counting loop with `paginator.total_posts` (jekyll-paginate-v2's own correctly-scoped count, confirmed via its `Paginator` class `attr_reader`). Verified locally (via the same temporary test-post build) that `article-list.html`'s existing `title_pt`/English-URL-fallback rendering logic works unchanged against whatever posts `paginator.posts` now contains — no changes were needed there since it already keyed off `_article.lang`/`_article.title_pt`, not the listing mechanism

## 4. Verify via a real build (branch + PR)

- [x] 4.1 Push to a branch and open a PR; confirm the new custom build step succeeds (not just that a check exists — confirm it's actually running `bundle exec jekyll build`, not the old wrapped action) — PR #15; logs confirm `ruby/setup-ruby` installed Ruby 3.3.12 + Bundler 4.0.20 from the committed `Gemfile.lock`, then `bundle exec jekyll build` ran for real (matches local build output exactly, same SASS deprecation warnings, "done in ~1s")
- [x] 4.2 Download the built artifact and check: `/`, `/page2` (and further pages if any) contain only English posts, correctly split across pages; `/pt/`, `/pt/page2` (etc.) contain the correctly deduped Portuguese listing, split across pages; the pagination widget's stats/page-links match reality on both — confirmed: 6 correctly-scoped posts on each home page, no `page2` generated for either (6 < `per_page: 8`, correct). **Found and fixed 2 real regressions** during this check: (1) `jekyll-paginate-v2` overwrites `page.data['title']` with the raw site title on any page it paginates that has no explicit `title:` front matter — clobbered `index.html`'s theme-locale-resolved "Home" title into "Vitor Casadei's Blog - Vitor Casadei's Blog"; (2) it also mutates `page.data['permalink']` internally in a way that changed `page.url` from `/pt/` to `/pt/index.html` on `pt/index.html`, changing its giscus `data-term` (canonical URL/sitemap were unaffected, only the raw `page.url` Liquid reference). Fixed both by giving each home page an explicit `title:`/`key:`, matching the pattern already used elsewhere (about.md/sobre.md) — verified fixed in a second real PR build
- [x] 4.3 Confirm no unrelated pages regressed (diff a sample of untouched pages against a master build) — full sweep across every page in both builds; all diffs were either the intended pagination/title/term changes, or pre-existing cosmetic differences from the Jekyll/kramdown/Rouge version bump (footnote ARIA-role attribute placement, PowerShell syntax-highlight token class, and `mark_lines` code-highlighting now actually rendering — an incidental improvement, not a regression)
- [x] 4.4 Merge to `master`; confirm the `deploy` job succeeds using the new build step

## 5. Confirm live

- [ ] 5.1 Load `/`, `/page2`, `/pt/`, and `/pt/page2` (if generated) live and confirm each shows the correct, language-scoped set of posts
- [ ] 5.2 Confirm `openspec validate paginate-posts-by-language --strict` passes
