## 1. Locale data foundation

- [x] 1.1 Add a `pt-BR` block to `_data/locale.yml` (translating SUBSCRIBE, READMORE, SEARCH, CANCEL, VIEWS, LAST_UPDATED, PREVIOUS, NEXT, ARTICLE_DATE_FORMAT, ARTICLE_LIST_DATE_FORMAT, STATISTICS, LICENSE_ANNOUNCE, POST_ON_GITHUB, FOLLOW_ME, FOLLOW_US, EMAIL_ME, EMAIL_US, COPYRIGHT_DATES), following the same key structure as the existing `en`/`fr`/`tr` blocks (also aliased a plain `pt` key to the same block, matching the pattern other languages use)
- [x] 1.2 Add `defaults:` scope to `_config.yml`: `path: "_posts/pt"`, `type: posts` → `lang: pt-BR`, `permalink: /pt/:year/:month/:day/:title.html`; verify the file still parses as valid YAML

## 2. Portuguese structural pages

- [x] 2.1 Create `pt/index.html` (`layout: home`, `lang: pt-BR`, `permalink: /pt/`) — **finding**: `home.html`'s default `articles.data_source: paginator.posts` depends on classic `jekyll-paginate`, which only paginates a single site-root index page and would not generate a second paginator for `/pt/`. Overrode `articles.data_source: site.posts` on this page instead (unpaginated; acceptable given how little Portuguese content exists so far), using the layout's existing per-page data-source override support
- [x] 2.2 Create `pt/archive.html` (`layout: archive`, `lang: pt-BR`, `permalink: /pt/archive.html`)
- [x] 2.3 Create `pt/about.md` (`layout: article`, `lang: pt-BR`, `permalink: /pt/about.html`) with a drafted Portuguese translation of the current English About content (kept honestly parallel to the English placeholder: "Em breve" ["Soon"], since `about.md` itself is still just "Soon" pending `finish-about-page` — will need a real translation once that change lands)

## 3. Migrate the existing Portuguese post into the formal structure

- [x] 3.1 Move `_posts/2026-09-11-assistente-pessoal-local-hacktown-2026.md` into `_posts/pt/`, keeping the same filename/date (new URL becomes `/pt/2026/09/11/assistente-pessoal-local-hacktown-2026.html`)
- [x] 3.2 Remove its now-redundant `hide_from_index: true` (superseded by language-scoped filtering from task 4) and `sitemap: false` (Portuguese content should now appear in the Portuguese sitemap/listings) front matter
- [x] 3.3 Add `translation_key: local-personal-assistant-hacktown-2026` (or another shared value) to both this post and its English counterpart's front matter
- [x] 3.4 Update the manual cross-link paragraphs in both posts' bodies (added in the earlier PT-post change) to point at the post's new URL, or replace them with reliance on the new language switcher (task 6) — avoid two competing cross-link mechanisms on the same pages (removed both manual cross-link paragraphs; the sitewide switcher from task 6 takes over this job)
- [x] 3.5 Update `_data/variables.yml`'s giscus config usage: no change needed here, but confirm the `custom.html` mapping change (task 7) correctly shares a thread between this pair (no `_data/variables.yml` giscus references exist — confirmed via grep; giscus config lives entirely in `_config.yml`/`custom.html`, addressed in task 7)

## 4. Language-scoped listings (supersedes `hide_from_index`)

- [x] 4.1 Update `_includes/article-list.html` to filter `include.articles` by `(article.lang | default: 'en') == (page.lang | default: 'en')` instead of the `hide_from_index` check — **finding**: a plain equality filter can't satisfy both this task's premise and the spec's requirement that non-English listings show untranslated English posts (via `title_pt` fallback). Implemented asymmetric logic instead: English pages (`page.lang` unset or `en`) show only `lang: en` posts; non-English pages show every post except an English post whose `translation_key` is already represented by a same-language sibling post (avoids double-listing a pair). Non-English rendering also swaps in `article.title_pt` when present. Applied to all 4 title-render call sites (item/brief/grid branches)
- [x] 4.2 Update `_includes/tags.html`'s "Show All" count and `_includes/paginator.html`'s post-count stat the same way — mirrored the identical asymmetric counting loop (over `site.posts`) in both files, replacing the old `hide_from_index != true` count expressions
- [x] 4.3 Update `_includes/search-providers/default/search-data.js` the same way, so the client-side search index is also language-scoped — **finding**: this file renders once into a single sitewide `assets/search.js` asset (its own front-matter-only page, not re-included per viewing page), so there is no "current listing page" `page.lang` to scope by the way the other three files have. The theme's search UI is also global with no language toggle. Applied the only part of this task that still applies: removed the dead `hide_from_index` check (that flag no longer exists post-migration) so the index simply lists every post/page by its own native title, unfiltered by language — search remains a global, language-agnostic feature
- [x] 4.4 Verify (via a test build) that the English archive/home no longer list the Portuguese post, and `/pt/archive.html`/`/pt/` list it correctly — deferred to task group 8's real branch+PR+Actions build (local Jekyll builds are broken on this machine's Ruby version); will confirm there alongside the other build-verification tasks

## 5. Post title translation

- [x] 5.1 Draft `title_pt` front matter for the 5 English-only posts (Hello World!; Scrum for Applied Research; Upgrading Network and Security Cameras on a Rural Property (on a budget); Improving Home Security using AI; SCRUM4Research - Lessons Learned and Tips on Sprint Planning) — flagged for owner review, same treatment as the `finish-about-page` change's drafted content. Drafted: "Olá, Mundo!"; "Scrum para Pesquisa Aplicada"; "Atualizando Rede e Câmeras de Segurança em uma Propriedade Rural (com orçamento reduzido)"; "Melhorando a Segurança Residencial usando IA"; "SCRUM4Research - Lições Aprendidas e Dicas sobre Planejamento de Sprint"
- [x] 5.2 Update the Portuguese archive/home listing template logic to render `article.title_pt | default: article.title` when the current page's `lang` is Portuguese, and to link to the article's own URL (which, for untranslated posts, is simply their existing English URL — no separate Portuguese permalink exists for them) — already satisfied by the `_article_title`/`_article_url` logic added to `_includes/article-list.html` in task 4.1 (title_pt fallback + native `_article.url` link); no separate change needed

## 6. Language switcher and language-aware nav

- [x] 6.1 Reintroduce a local `_includes/header.html` override (based on the theme's current version, fetched from `kitian616/jekyll-TeXt-theme` at the pinned `v2.2.6` tag via `gh api`) adding a switcher link: to `page.translation_key`'s counterpart URL when the current post has one, else to `/pt/` (from an English page) or `/` (from a Portuguese page). Rendered as a `navigation__item` list entry labeled "PT"/"EN" so it inherits the theme's existing nav-item styling with no new CSS needed
- [x] 6.2 In the same override, make the "Archive"/"About" nav item hrefs language-aware: prefixes each `site.data.navigation.header` item's URL with `/pt` whenever `page.lang` isn't English (`/pt/archive.html`, `/pt/about.html`), else the existing English URLs. Also added `pt-BR` (aliased `pt`) label translations ("Arquivo", "Sobre") to `_data/navigation.yml`, following the same anchor-alias pattern already used for `fr`/`tr`, so the nav labels themselves render in Portuguese
- [x] 6.3 Verify the switcher and nav links render correctly on: an English post with a translation, an English post without one, the Portuguese post, `/pt/`, `/pt/archive.html`, `/pt/about.html`, and the English equivalents — confirmed via the PR #8 build artifact and again live: paired posts' switchers link to each other's own URL in both directions; unpaired English pages' switcher points to `/pt/`; Archive/About nav hrefs and labels render correctly in both languages

## 7. Shared comments across paired posts

- [x] 7.1 Change `_includes/comments-providers/custom.html`'s `data-mapping` from `{{ site.comments.giscus.mapping | default: 'pathname' }}` to `"specific"`, and add a `data-term` — **finding**: a real build showed pages without front-matter `key` (404.html, archive/home layouts) rendered `data-term=""` under `page.translation_key | default: page.key`, an empty-term regression versus the old `pathname` mapping which needed no term. Fixed with a further `| default: page.url` fallback, so every page keeps a non-empty, unique term unless it deliberately shares one via `translation_key`
- [x] 7.2 Update `_config.yml`'s `comments.giscus.mapping` value (or remove it, since it's no longer read) to avoid dead/misleading config — removed the now-unused `mapping: pathname` line; verified `_config.yml` still parses
- [x] 7.3 Verify via a test build that the English and Portuguese HackTown posts both compute the same `data-term`, and an unpaired post (e.g. `hello-world`) computes its own unique term unaffected by this change — confirmed in the PR #8 build artifact and again live: both HackTown posts render `data-term="local-personal-assistant-hacktown-2026"`; `hello-world` renders its own `data-term="hello-world"`

## 8. Verify via a real build (branch + PR)

- [x] 8.1 Push to a branch and open a PR; confirm the `build` GitHub Actions check passes — PR #8, `build` passed on both the initial push and the follow-up giscus `data-term` fix
- [x] 8.2 Download the built artifact and check: `/pt/index.html`, `/pt/archive.html`, `/pt/about.html` exist and render in Portuguese; the English archive excludes the Portuguese post and vice versa; the language switcher appears and links correctly on a sample of pages; the giscus embed's `data-term` matches between the paired EN/PT posts — all confirmed. **Found and fixed during this step**: `data-term` rendered empty on pages without a front-matter `key` (404.html, home/archive layouts) — see 7.1's note
- [x] 8.3 Confirm no unrelated pages regressed (diff a few untouched pages against the pre-change build, similar to the verification done in `migrate-to-theme-gem`) — diffed every non-`/pt/` HTML/XML page against a master build; all diffs were the expected sitewide additions (language switcher, giscus mapping/term) or the deliberate HackTown post edits. One incidental improvement: a pre-existing malformed `</ul></section>` artifact in `archive.html` (caused by `hide_from_index` breaking the article-list template's `forloop.first` check) is now gone since the first listed post is no longer skipped
- [x] 8.4 Merge to `master`; confirm the `deploy` job succeeds — PR #8 squash-merged, `deploy` succeeded

## 9. Confirm live and finalize

- [x] 9.1 Load `/pt/`, `/pt/archive.html`, `/pt/about.html` on the live site and confirm they render correctly — confirmed; **found and fixed** a follow-up issue (PR #9): `/pt/` and `/pt/archive.html` showed English `<title>` text ("Home"/"Archives") because the theme's `home.html`/`archive.html` layouts have no `pt-BR` entry in their own locale maps. Fixed by setting an explicit `title:` on each page (`Início`, `Arquivo`), same pattern as `pt/about.md`. Confirmed live afterward: all three pages show correct Portuguese titles
- [x] 9.2 Confirm the language switcher works live, in both directions, for both a paired and an unpaired page — confirmed: the EN HackTown post's switcher links to `/pt/2026/09/11/assistente-pessoal-local-hacktown-2026.html` and back
- [x] 9.3 Confirm live that the previously separate EN/PT giscus threads are now unified (new comments on either page appear on both) — confirmed structurally: both live pages render the identical `data-term="local-personal-assistant-hacktown-2026"`, which is what giscus uses to key the shared discussion; did not post a live test comment (out of scope for this session to leave a real public artifact). Accept that any prior comments under the old `pathname` mapping are no longer linked (documented breaking change)
- [x] 9.4 Ask the owner to review the drafted `title_pt` values and the Portuguese About page draft, and edit as needed — asked in this session's final summary to the user
- [x] 9.5 Confirm `openspec validate add-bilingual-pt-support --strict` passes — confirmed: "Change 'add-bilingual-pt-support' is valid"
